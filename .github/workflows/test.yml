name: Workflow
on: 
  workflow_dispatch:
    inputs:
      goamd64:
        description: 'Test 2'
        required: false
        type: string
      include-services:
        description: 'Test 2'
        required: false
        type: string

jobs:
  test: # Job ID
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: Build Go
        uses: ./.github/actions/build-go-services
        with:
          goamd64: ${{ inputs.goamd64 }}
          include-services: ${{ inputs.include-services }}
      - name: Build docker images
        run: echo "Creating..."
  # create-image:
  #   needs: test
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Create Image
  #       run: echo "Creating..."